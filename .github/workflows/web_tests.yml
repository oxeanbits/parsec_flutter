name: Web Integration Tests

on:
  push:
    branches: [ main, develop, implement_parsec_web ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  web_tests:
    name: Web Tests (WebAssembly)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    strategy:
      matrix:
        flutter_version: [3.19.0, 3.24.0]
        
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ matrix.flutter_version }}
        channel: stable
        cache: true
    
    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y emscripten
    
    - name: Install Chrome for Web testing
      uses: browser-actions/setup-chrome@v1
      with:
        chrome-version: stable
    
    - name: Setup Web Assets
      run: |
        chmod +x setup_web_assets.sh
        ./setup_web_assets.sh
    
    - name: Get Flutter dependencies
      run: |
        cd parsec_platform_interface && flutter pub get
        cd ../parsec_web && flutter pub get
        cd ../parsec && flutter pub get
    
    - name: Analyze parsec_web package
      run: |
        cd parsec_web
        flutter analyze --no-fatal-infos
    
    - name: Analyze main parsec package
      run: |
        cd parsec
        flutter analyze --no-fatal-infos
    
    - name: Build WebAssembly module
      run: |
        cd parsec_web/lib/parsec-web
        chmod +x build.sh
        ./build.sh
    
    - name: Run parsec_web JavaScript tests (Node.js)
      run: |
        cd parsec_web/lib/parsec-web
        npm install
        npm test
    
    - name: Run Flutter Web integration tests
      run: |
        cd parsec
        flutter test --platform chrome test/parsec_test.dart
    
    - name: Run Web-specific integration tests
      run: |
        cd parsec
        flutter test --platform chrome test/parsec_web_integration_test.dart
    
    - name: Build Web example (validation)
      run: |
        cd parsec/example
        flutter build web --release
    
    - name: Upload test artifacts
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: web-test-artifacts-${{ matrix.flutter_version }}
        path: |
          parsec/coverage/
          parsec_web/lib/parsec-web/coverage/
          parsec/test-results.xml
        retention-days: 3

  web_compatibility:
    name: Web Compatibility Tests
    runs-on: ubuntu-latest
    needs: web_tests
    timeout-minutes: 10
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: 3.24.0
        channel: stable
        cache: true
    
    - name: Setup Web Assets
      run: |
        chmod +x setup_web_assets.sh
        ./setup_web_assets.sh
    
    - name: Test parsec-web npm package
      run: |
        cd parsec_web/lib/parsec-web
        npm install
        npm run test:unit
        npm run test:integration
    
    - name: Validate WebAssembly module
      run: |
        cd parsec_web/lib/parsec-web
        file wasm/equations_parser.wasm
        ls -la wasm/
    
    - name: Test JavaScript imports
      run: |
        cd parsec_web/lib/parsec-web
        node -e "const E = require('./index.cjs'); console.log('✅ CommonJS works')"
        node --input-type=module -e "import('./index.mjs').then(()=>console.log('✅ ES6 works'))"

  performance_tests:
    name: WebAssembly Performance Tests
    runs-on: ubuntu-latest
    needs: web_tests
    timeout-minutes: 8
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        submodules: recursive
    
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: 3.24.0
        channel: stable
        cache: true
    
    - name: Setup Web Assets
      run: |
        chmod +x setup_web_assets.sh
        ./setup_web_assets.sh
    
    - name: Run WebAssembly performance benchmarks
      run: |
        cd parsec_web/lib/parsec-web
        npm install
        npm run test:performance
    
    - name: Upload performance results
      uses: actions/upload-artifact@v4
      with:
        name: performance-results
        path: parsec_web/lib/parsec-web/benchmark-results.json
        retention-days: 7